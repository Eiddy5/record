package table.selection;

import table.core.Table;

public class SelectionCorrecter {

    /**
     * 将selection 放到table中 重新矫正
     * 如果选择的cell包含被合并的大 cell，需要将原有的selection的start和end矫正为扩大后的start和end
     */
    public Selection corrector(Table table, Selection selection) {
        // todo 完成矫正算法

        return selection;
    }

}
