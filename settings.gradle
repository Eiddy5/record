rootProject.name = 'record'
dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
include(
        'db',
        'netty',
        'pptEngine',
        'spring',
        'httpServer',
        'dubbo',
        'hazelcast-map'
)
