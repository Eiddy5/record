package org.record.mode.chain.checker;

import org.record.mode.chain.model.TaskEntry;

public class TaskStatusChecker extends SimpleTaskChecker {
    @Override
    public void check(TaskEntry taskEntry) {

    }
}
